#Для того чтобы развернуть проект выполните команды
```
1. sudo npm install -g purescript --unsafe-perm=true

2. npm install -g pulp bower

3. pulp init
```

###Запуск командной строки выполнения
```
pulp repl
```
